# 安全政策

## 支持范围

以下版本会收到安全更新：

| 版本 | 支持情况 |
|---|---|
| 最新发布版本（当前 5.2.11） | ✅ 支持 |
| 上一个次版本 | ✅ 支持 |
| 更早版本 | ❌ 不受支持 |

## 报告漏洞

**请勿在公开渠道（Issue、讨论区）披露安全漏洞。**

请将安全漏洞通过以下任一方式私下报告：

- **GitHub Security Advisory**：在仓库页打开 **Security → Report a vulnerability**
- **邮件**：请在 Issues 中留言询问维护者的安全联系邮箱（不公开邮箱地址以防垃圾邮件）

### 报告内容建议

- 漏洞类型与影响范围
- 复现步骤（尽量最小化）
- 受影响的版本
- 可能的修复建议（可选）

### 响应时间

- 收到报告后 **48 小时内**确认
- **7 天内**给出初步评估
- 修复发布前不会公开漏洞细节

## 安全注意事项（开发者）

- API Key 通过本地文件系统插件存储，代理只监听 `127.0.0.1`（本地回环）
- 不要放宽 `tauri.conf.json` 中的 CSP 配置
- 敏感配置（签名密钥、API Token）禁止提交到仓库，使用环境变量或 CI 密钥
- 修改涉及密钥存储 / 代理认证 / 路径处理的代码时，请重点自查安全问题
